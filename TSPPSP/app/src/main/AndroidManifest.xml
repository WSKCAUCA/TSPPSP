<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.caucaragp.worldskills.tsppsp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconox144"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconox144"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.MenuPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controllers.MenuProyecto" />
        <activity
            android:name=".controllers.TimeLog"
            android:label="@string/title_activity_timer_log" />
        <activity
            android:name=".controllers.DefectLog"
            android:label="@string/title_activity_defect_log" />
        <activity
            android:name=".controllers.ProjectPlanSummary"
            android:label="@string/title_activity_project_plan_summary"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>